/**
 * Stable auth error messages for REST/tRPC status mapping.
 * Kept free of Better Auth so http-error can import without pulling the auth stack.
 */
export const OWNER_FORBIDDEN_MESSAGE = "This account is not the instance owner"
export const UNAUTHORIZED_MESSAGE = "Unauthorized"
